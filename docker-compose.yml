services:
  api:
    platform: linux/amd64
    image: cooperative-api:latest
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8089:8089"
    restart: unless-stopped
